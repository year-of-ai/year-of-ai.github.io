---
description: "Append the current session's actions to the Evolution Log (section 8) of seed.md as a reproducible record. Append-only; never touches sections 1–7."
argument-hint: "<short session label> — optional; defaults to today's date + a summary"
---

Append a new entry to the **Evolution Log (section 8)** of `seed.md` recording what was done this session. Do **not** modify sections 1–7 — those are the DNA, regenerated by the **sync-seed** skill.

Follow the canonical procedure in `.github/prompts/encode-seed.prompt.md`:

1. Gather what happened this session — tools run (key inputs + outcome), files created/modified (path + one-line description), decisions made. Use `$ARGUMENTS` as the session label, or derive one from the primary action.
2. Format the entry per the existing Evolution Log style (`## <Label> — <YYYY-MM-DD>` + summary + tool calls + file list).
3. Append it at the **bottom** of section 8, after the last existing entry. Do not edit earlier entries or sections 1–7.
4. Report the label, date, number of tool calls recorded, and files listed.

Session label: $ARGUMENTS
