# seed.md — Repository DNA for "{{SUBJECT}}"

> **Edit rules**: Sections 1–7 are generated by the `sync-seed` skill — do not hand-edit.
> Section 8 (Evolution Log) is append-only, written by `encode-seed`. Never regenerate or reorder it.

---

## Section 1 — Concept Definition

```yaml
concept:
  subject: "{{SUBJECT}}"                      # e.g. "the year 1776", "an organization", "a technology"
  scope: >
    {{ONE-PARAGRAPH SCOPE STATEMENT — what is in vs. out of bounds for this knowledge base.
    Name the dimensions the subject is decomposed along and the boundary that keeps facts in-scope.}}
  taxonomy:                                    # 4–8 mutually-exclusive categories; each owns a folder slug
    - name: "{{Category One}}"
      slug: "{{category-one-slug}}"
    - name: "{{Category Two}}"
      slug: "{{category-two-slug}}"
    # … add the remaining categories
  source_strategy:
    encyclopedic: "{{Broad reference sources, e.g. Wikipedia, Britannica}}"
    specialist:
      - "{{Authoritative specialist source 1}}"
      - "{{Authoritative specialist source 2}}"
    minimum_sources: 2                         # every fact verified against ≥ this many sources
  conventions:
    knowledge_table: "## Notable {{Items}} of {{SUBJECT}}"   # README table heading
    file_path: "<category-slug>/<topic-slug>.md"
    frontmatter: ["title", "date", "category"]
    tone: "factual, neutral, encyclopedic, third person"
```

---

<!--
  Sections 2–7 (Identity & Mission, Architecture, Content Inventory, Structure Inventory,
  Growth Loop, Rebuild Procedure) are GENERATED by the `sync-seed` skill on the first tick.
  Do not author them by hand — `/genesis` and the first `/grow` produce them from Section 1.
-->

## Section 8 — Evolution Log

> **Append-only.** Each entry is written by `encode-seed` at the end of a growth session.
> Never edit, reorder, or regenerate prior entries.

<!-- The first entry is appended by /genesis. -->
